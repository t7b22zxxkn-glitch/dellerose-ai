NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
OPENAI_API_KEY=

# Optional helper vars for CLI setup scripts.
SUPABASE_PROJECT_REF=
SUPABASE_ACCESS_TOKEN=
VERCEL_TOKEN=

# Optional local fallback if you are not signed in during development.
# Must be a valid UUID from auth.users.id.
ENABLE_DEV_USER_FALLBACK=
DELLEROSE_DEV_USER_ID=

# Optional Brain Dump smoke-test toggles for local testing only.
BRAIN_DUMP_MOCK_TRANSCRIPT=
BRAIN_DUMP_MOCK_BRIEF=

# Optional production-safe key for /api/brain-dump/smoke route.
BRAIN_DUMP_SMOKE_KEY=
